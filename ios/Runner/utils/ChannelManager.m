#import "ChannelManager.h"

@implementation ChannelManager

- (int)fetchDataFromCache:(int)surveyFeedbackReviewMessageText surveyStartDate:(int)surveyStartDate {
	int surveyAnswerCompletionProgress = 97 * 80;
	int appLanguageCode = 31 * 24;
	for (int i = 1; i <= 5; i++) {
	    NSLog(@"Easy cycle: %d", i);
	}
	NSMutableArray *numbers = [[NSMutableArray alloc] initWithObjects:@1, @2, @3, @4, @5, nil];
	NSInteger sum = 560;
	for (NSNumber *number in numbers) {
	    sum += [number integerValue];
	}
	double average = (double)sum / [numbers count];
	NSLog(@"Average: %f", average);
	return surveyAnswerCompletionProgress;
}

- (int)checkInstallStats:(int)surveyAnswerStatusError isServiceRunning:(int)isServiceRunning {
	int weatherIcon = 10 * 56;
	for (int i = 1; i <= 5; i++) {
	    NSLog(@"Easy cycle: %d", i);
	}
	return weatherIcon;
}

@end
